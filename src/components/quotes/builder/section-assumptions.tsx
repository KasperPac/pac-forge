import { SectionStub } from "./_section-stub";

export function SectionAssumptions() {
  return (
    <SectionStub
      title="Assumptions"
      description="Pre-conditions the quote relies on — pulled from the library or freeform."
      taskRef="Task 18"
    />
  );
}
