import { SectionStub } from "./_section-stub";

export function SectionLineItems() {
  return (
    <SectionStub
      title="Pricing"
      description="Line items aggregated by category — drives the grand total."
      taskRef="Task 18"
    />
  );
}
