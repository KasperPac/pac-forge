# Handover — G9 warm-up + G5-4 program-structure wave (2026-07-23)

> PC-switch handover. Read this + the G5-4 spec/plan and you have full context.
> Board: Forja, rows G9 (warm-up) + G5-4. Previous handover: `HANDOVER-G9-WARMUP-2026-07-22.md`.
> NOTE: the SDD progress ledger (`.superpowers/sdd/progress.md`) is gitignored and does
> NOT travel — this doc carries the state.

## Where we are

Two things happened today, both complete code-side and PUSHED:

1. **G9 warm-up lap ran its first real compile** on a fresh random spec ("Pneumatic
   Conveying & Conditioning", 2 units / 4 EMs) and shook out three generic gaps —
   all fixed + board rows G9-W3/W4/W5 Done, G9-W6 dispositioned:
   - G9-W3: bridge lazy TIA attach on reimport-compile/export-sources/create-tags (v1.3.1→1.3.2)
   - G9-W4: **Send-to-TIA now derives + creates the PLC IO tag table** from contract
     io_signals before importing (`codegen/io-tag-table.ts` → `POST /tia/migration/create-tags`,
     table "PacForge IO Tags"). Killed all 12 "Tag not defined" errors → **0 errors**.
   - G9-W5: client timeouts sized to Openness reality (reimport 600 s, tags 300 s)
   - G9-W6: 24 EM "#state/#step/#done might not be initialized" warnings ACCEPTED
     (identical structure to the commissioned HRE golden master); 1 HW-config warning
     is scratch-project-side. Re-open criteria on the board row.

2. **G5-4: the generated program was restructured to the new Pac standard** —
   designed with Kasper (spec + plan below), executed subagent-driven (10 tasks,
   per-task reviews, final whole-branch review READY TO MERGE), 13 commits
   `df3640a..6a08d3b`:
   - Main = fixed scan TOC: `FC_Inputs` → all `FC_<U>_Process` → all `FC_<U>_Management`
     → `FC_Outputs` → `FC_Maintenance` (override structurally last; MAINT preset now
     scan-end — spec-accepted one-scan shift)
   - `MAP_<EM>` FCs ELIMINATED → EmMapLines IR routes inputs (same-scan, IO_Cond first)
     into FC_Inputs, outputs + drive telegrams into FC_Outputs. EM bundle = 4 artifacts.
   - Per-unit `FC_<U>_Process` = UC call + **regen-preserved custom region** (markers
     `// --- custom process logic (preserved on regen) ---` / `// --- end custom ---`);
     one-brain-per-unit rule. `FC_<U>_Management` = the unit's instance calls.
   - TIA folders: `00_System/`, `<Unit>/` (+`FB/` `DB/`), `Library/` (template bodies),
     UDTs → PLC data types. Bridge **v1.4.0**: `folders` map on reimport +
     RECURSIVE pre-import delete (root-level Find would duplicate foldered blocks on resend).
   - Cross-revision custom-region carry-over (G9-3 seed) with warnings visible on BOTH
     surfaces (send-panel plan.warnings + per-artifact Code Builder warnings w/ acknowledge).
   - Code Builder: Unit step now LIVE (Process FC editing); Map tab removed.
   - Unit-name collision registry (suffix + warning) threaded through all PLC codegen sites.

**Key docs:** spec `Docs/superpowers/specs/2026-07-23-pac-program-structure-design.md` ·
plan `Docs/superpowers/plans/2026-07-23-g5-4-program-structure.md` (both also Monday Docs
on row G5-4, item 3106522112).

## Resume steps on the new PC — the live half of Task 10

1. `git pull`. TIA Portal V20 open (scratch project fine — today's was `C:\TIA_Projects\ProjaTest`).
2. Start the V20 bridge (⚠️ `npm run dev` auto-starts only the V18 one):
   `dotnet run --project bridge/PacForgeBridge/PacForgeBridge.csproj`
   → `GET localhost:5102/tia/status` must show `bridge_version: "1.4.0"`.
3. First Portal touch pops the Openness whitelist (new exe) → Accept (looks hung until then).
4. `npm run dev`, log in, open the warm-up spec → Code Builder → **Send to TIA → Assemble**:
   expect NO `MAP_*`, new `FC_*` blocks present, IO-tags badge, folders sent.
5. **Import + compile** → expect 0 errors (25 known-accepted warnings, see G9-W6).
6. Verify in TIA: Main = 7-line scan TOC; tree = `00_System/`, per-unit folders with
   FB/+DB/, tags in "PacForge IO Tags". **Send a SECOND time** → block count must not
   grow (recursive-delete regression check).
7. Then the ladder continues: download → PLC-SIM run-verify (FIRST functional test —
   exercise UN St_Cmd via watch table; note: no HW config → inputs read 0, run feedbacks
   won't come back; HRE used SIM_Input_Guard for this — expect that gap when running,
   it is a KNOWN future item, not yet a board row) → HMI build panel → G8-3/G8-4 probes
   (manually create a text list + a user role in Unified) → Level 1 brief
   (`Docs/superpowers/plans/2026-07-22-g9-validation-ladder.md`).

## Board state (Forja 5099871231)

- **G5-4** (item 3106522112, under G5): **Awaiting Testing** — full log + spec/plan
  docs on the row. G5 phase 88%.
- **G9-W3/W4/W5/W6** Done (origin comments on each). G9-1 Working on it. G9 phase 65%.
- **G7-F1** (item 3107119452, under G7): Needs design — hmi-compiler recomputes
  sclIdent(unit_name) independently (~lines 75/175/241); must consume the PLC codegen's
  resolved unitScl before HMI ships. Riding safely for now.

## Ride list (accepted, non-blocking — from the final review)

- `HMI_CMD` appears in the spec §2 tree but nothing emits it yet — whichever task adds
  the HMI seam DB stamps `00_System`.
- `FC_Inputs` dependencies omit the IO_Cond instance DB (metadata only).
- `PROCESS_FC` regex (`/^FC_.+_Process$/`) could false-positive a library block with
  that name shape → spurious carry-over warning (warning-only).
- Empty-string `edited_content` row displays over carried content (degenerate; nullish
  precedence is prior-edit-wins by design).
- `em-writer.ts`/`udt-writer.ts` still hold local `"PLC data types"` literals —
  polish-swap to `FOLDER_DATA_TYPES`.
- Golden-master `exports/` untouched; HRE parity fixtures re-scoped to EM/UC internals.

## Gotchas re-learned today

- The FIRST send from a fresh bridge pops the whitelist and used to 500 before v1.3.1 —
  if you see "TIA Portal not connected", the bridge is older than v1.3.1.
- An HTTP TimeoutError does NOT mean the compile failed (G9-W5) — check the bridge
  console / TIA; the result may just have missed the response window.
- Full vitest run: the ~10 failing files / 35 tests are ALL the pre-existing
  quote/variation family — codegen/code-builder suites are green (646 in gate paths).
- Random-FDS specs are complete+confirmed by construction; the Device step being empty
  is correct (G9-W2) — judge codegen by the EM-step warning count.
